# 📜 更新日志

- [📜 更新日志](#-更新日志)
  - [v1.3.2](#v132)
  - [v1.3.1](#v131)
  - [v1.3.0](#v130)
  - [v1.2.0](#v120)
  - [v1.1.1](#v111)
  - [v1.1.0](#v110)
  - [v1.0.3](#v103)
  - [v1.0.2](#v102)
  - [v1.0.1](#v101)
  - [v1.0.0](#v100)
  - [Chrome 重构](#chrome-重构)
  - [预览版](#预览版)


---

## v1.3.2
- 更新检查面板：检测到新版本时，提示文字中的「前往更新页面」改为可点击链接，可直接跳转 GitHub 发布页

## v1.3.1
- 修复自动同步误判冲突：保存设置时用页面内存的 settings 整体覆盖 storage，把同步元数据（`lastRemoteModified` / `lastSyncAt` / `lastSyncedFingerprint` / `configVersion`）回退成旧值，导致下一次同步把远端误判为已变更而产生假冲突
- 保存设置改为合并 storage 最新同步元数据，用户新改字段仍以页面为准；各保存入口统一处理
- 内置壁纸重命名为数字顺序（`1.webp` ~ `15.webp`），预设壁纸列表同步更新

## v1.3.0
- 设置面板重构为分类导航：外观设置 / 数据与同步 / 其他 / 更新检查 / 关于 五个独立 Tab 卡片，切换更清晰
- 设置面板宽度增加至 460px，布局更从容；分类 Tab 采用独立圆角卡片样式，与内容区分离
- 外观设置内细分「主图设置 / 卡片设置 / 卡片背景设置 / 字体设置」四个分区，用分隔线组织
- 卡片背景色改为预设色块 + 现代化取色器：新增黑白灰及多色系预设，并支持 H/S/L/HEX 精确输入
- 新增自定义颜色取色器弹窗：RGB 圆形色相轮 + 饱和度/亮度区，磨砂玻璃质感，实时预览
- 更新检查 / 关于面板：一键检测 GitHub 最新发布版本、查看版本号、仓库、帮助与隐私说明
- 搜索引擎与书签图标来源设置迁移至「其他」分类；移除无实际作用的「卡片描述」开关
- 修复自动同步：设置保存后未触发上传的问题（本地变更判定错误）
- 修复书签更改时重复触发两次同步的问题（同步元数据写入被误判为「设置更改」）
- 新增内置壁纸 wallhaven-e7jj6r.webp

## v1.2.0
- WebDAV 同步由「单向上传」升级为「双向智能同步」：每次同步先通过 HEAD / PROPFIND 比对远端 `Last-Modified` / `Content-Length`，自动决定上传、下载或跳过
- 新增同步方向策略：双向（默认）/ 仅上传 / 仅下载
- 新增冲突保护：本地与远端都改过时提示「本地较新 / 远端较新 / 冲突」，可一键选择保留方向；远端覆盖将使本地书签变化超过 20% 时强制要求用户确认，杜绝静默覆盖
- 导出 / 同步的配置文件新增书签数量统计（`bookmarkCount` / `folderCount`）与内容指纹、版本号、导出时间
- 新增「同步书签内容」开关：取消勾选后仅同步设置与卡片布局
- 设置页新增同步状态栏：实时显示「上次成功时间 / 下次定时备份时间 / 远端状态」，并支持一键检查同步状态
- 新增可选「版本化备份」：每次上传额外保存带时间戳的快照（如 `ggbookmark-config.20260809-153000.json`），自动保留最近 10 份；设置页可查看全部历史版本（含时间与书签数量），并一键恢复任意版本
- 手动按钮整合为「立即同步」（按策略自动判断）+「强制上传 / 强制下载」（不做比较直接覆盖）
- 自动同步（定时 / 书签变更 / 浏览器启动）全部接入双向策略与冲突保护；后台检测到冲突时通过消息提示用户到设置页处理

## v1.1.1
- 版本号统一管理：`manifest.json` 为唯一权威来源，`lib/store.js` 的 `GG.VERSION` 与起始页页脚运行时自动读取，发布时只需修改一处
- 起始页底部版本号改为指向 GitHub 仓库的可点击链接
- 帮助页新增「所有书签浮窗」使用说明，并补充卡片 / 置顶栏 Ctrl 后台打开等快捷键说明
- 帮助页快捷键代码样式优化，`~` / `` ` `` 等键位更醒目

## v1.1.0
- 顶部工具栏最左侧新增「所有书签」按钮（GGicon 的 allBookmarks 图标），弹出居中浮窗：左侧文件夹导航树、右侧内容区，支持选中文件夹浏览、含子文件夹开关与即时搜索（多词匹配、关键词高亮、显示所在路径）
- 「所有书签」浮窗支持纯键盘流：`` ` `` / `~` 键唤起或关闭，输入即搜，↑↓ 选择，Enter 打开，Ctrl+Enter 后台打开，Esc 关闭
- 卡片书签与置顶栏新增 Ctrl/Cmd+左键、鼠标中键后台打开，页面停留在当前起始页
- 所有弹出菜单（viewMenu / seMenu / 右键菜单 / 整理页菜单等）背景与模糊统一为卡片同款磨砂风格，明亮主题下菜单自动换亮色实底
- 修复嵌套 backdrop-filter 失效导致 viewMenu / seMenu 无磨砂、内容看不清的问题（topbar 内嵌菜单改用实底背景）

## v1.0.3
- 修复 lib/store.js 中 GG.VERSION 与 manifest.json 版本号不同步的问题
- 更新日志移入独立的 CHANGELOG.md，README 中添加跳转链接
- 新增 release.md 发布流程文档，规范版本迭代与自动化发布

## v1.0.2
- 点击卡片上的「紧凑模式」或「适应高度」按钮时，仅更新当前卡片，不再刷新其他所有卡片

## v1.0.1
- 修复自定义壁纸与预设壁纸共用输入框导致的壁纸数据串扰问题（预设引用不再写入「图片来源」）
- 自定义壁纸改为存入 IndexedDB 而非超大 data URL，大幅提升保存 / 导出 / 同步速度
- 自定义壁纸不再限制大小，任意图片均可正常加载显示
- 本地上传按钮改用 GGicon 的 upload 图标，移除多余的「应用」按钮

## v1.0.0
- 密码改为加密存储，避免明文保存
- 修复拖动卡片高度时首帧直接跳为两倍高度的问题
- 顶部工具栏下方新增置顶书签栏
- 合并书签 / 文件夹编辑浮动窗口与整理页右键编辑
- 新增新建文件夹窗口
- 优化整理页右侧面板，修复拖拽到面板文件夹无效、多选删除仅删一项、撤销等问题
- 右侧面板顶部新增路径链路，可点击返回任意上级目录
- 修复卡片内部子项拖拽排序问题
- 新增顶部工具栏、卡片与置顶行的磨砂模糊与透明度滑块
- 新增浏览器启动时自动触发同步机制
- 书签更改后延迟 30s 再上传，避免频繁上传
- 修复卡片选择文件夹时「包含子文件夹」选项无效的问题
- 修复右键置顶书签编辑后卡片与收藏夹不同步的问题
- 卡片宽度固定为设定值，页面宽度变化 / 刷新后布局保持不变
- 修复 tooltips 不显示的问题
- 置顶栏横向放不下时自动换行
- 新增卡片设置文件夹时若已存在对应卡片则提示
- 置顶栏新增清除按钮
- WebDAV 密码输入框样式与用户名统一，间隔改为滑动调节（步距 1 分钟，范围 1–120）

## Chrome 重构
- 全面弃用 Firefox 兼容层，以 Chrome 原生 `chrome.*` API 重写
- 修复 Chrome 书签节点无 `type` 字段导致的文件夹无法显示问题（统一归一化注入 `type`）
- 重构文件结构，将起始页逻辑拆分维护

## 预览版
- 内置预设壁纸
- 支持一键导出 / 恢复配置（一步到位包含书签）
- 新增导入导出目标文件夹设置，下次导入自动读取，无需重复选择
- 预设壁纸优化，融合进上方背景按钮
- 支持明亮 / 黑暗模式切换，导入导出时同步保存加载
- 下拉菜单背景与文字色根据主题自动适配
- 新增 WebDAV 同步：支持设置地址、用户名、密码，可定时备份、检测设置更改 / 书签修改触发、手动上传 / 下载
- 书签图标来源设置，无图标时显示标题首字母
- 空白处右键菜单支持新建卡片，生成在右键所在列
- 卡片选择文件夹时默认全部折叠，无子文件夹时不显示箭头
- 整理书签页支持子项上下拖拽排序
- 修复右侧面板文件夹项数显示为 0 的问题
- 左侧导航与右侧内容均支持右键编辑 / 删除文件夹与书签
- 自定义壁纸按页面大小自动填充整个区域
- 整理书签页支持左右分栏导航与多内容页
- 左侧文件夹图标固定，折叠状态由箭头指示（展开朝下 / 折叠朝右），无子文件夹不显示箭头
- 整理书签页与主页图标优化
- 主页空白处支持新建空卡片，空卡片内可选择文件夹
- 底部显示 @firestaradmin
- 设置改为从右侧滑出面板，方便直接预览修改效果
- 修复背景模糊设置时边框泛白的问题
- 移除搜索栏右侧小三角，切换菜单统一在放大镜下方弹出
- 分类新增按钮改为仅在分类标签最右侧显示一个「+」
- 卡片底部拖拽区域改为毛玻璃圆角横杠，替代突兀的悬浮效果
- 支持在卡片内部拖动子项排序
- 卡片根据内容适应高度的按钮图标更直观，并根据开关状态切换
- 卡片拖拽落位增加直观横条反馈（同卡片子项拖拽）
- 右上角新增全局紧凑模式按钮，一键调整所有卡片
- 卡片拖拽优化：拖到目标列空间即可释放，无需贴靠卡片边缘，并增加直观的落位反馈
- 修复卡片右上角按钮弹出菜单被其他卡片遮挡的问题（置顶显示）
- 支持卡片自由拖拽，任意数量卡片均可排成同一列
- 自动适应后保持紧凑模式状态，不再自动关闭
- 卡片设置菜单改为在右侧弹出，并加深背景提升可读性
- 卡片右下角拖拽改为仅上下调节，卡片宽度统一在全局设置中调整
- 支持一列放置多个卡片，卡片可堆叠于任意一列
- 修复设置背景图后仍不显示的问题
- 自定义背景不再被模糊遮蔽，支持自定义模糊强度与透明度以完整呈现背景图
- 修复卡片右上角按钮图标不显示的问题
- 卡片子项支持整体高度调节：空间不足时域名自动移到标题同行并省略号显示，图标同步缩小
- 支持拖动卡片右下角调整卡片占用大小与宽度，同一列卡片宽度保持一致
- 新增按钮，根据子项数量自动调节卡片高度以显示全部子项
