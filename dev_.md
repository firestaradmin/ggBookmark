---
问题：
1、自定义背景后不显示图片，可能是背景模糊太大，遮住了，让用户可以自定义背景模糊程度和透明程度，来显示自定义的背景图
2、卡片右上角的按钮图标没显示
3、我希望卡片的子项整体高度可调，如果高度太小可以将右边下方的域名显示到标题同一行，如果显示不下就省略号。图标也同步调小一些。
4、我希望用户可以拖动调整卡片右下方来调节卡片占用的大小和宽度，但是同一列的卡片宽度是一致的
5、增加一个按钮，让卡片直接根据子项多少来调节高度，以便显示所有子项

---
问题：
1、卡片的设置按钮弹出面板我希望在右侧，另外弹出的面板背景太透明了看不清。
2、将卡片右下角拖拽改为只能上下拖拽吧，然后所有卡片的宽度在全局设置里调整
3、现在每一列只能放一个卡片，我希望卡片可以放在卡片下方，就是每一列可以放置多个卡片
4、设置了背景图，但是背景还是看不到图片，好像就没显示

---
我的项目地址~/code/ggBookmark，这是firefox书签管理工具扩展，现在有以下问题，帮我解决下
问题：
1、目前卡片右上角的按钮弹出的菜单没有置顶，被别的卡片遮挡了
2、我希望卡片可以随意拖动，哪怕只有两个卡片，也可以排成同一列，可以拖拽卡片到任意一列
3、现在右上方自动适应后，卡片的紧凑模式就关闭了，我希望保持状态

我的项目地址~/code/ggBookmark，这是firefox书签管理工具扩展，现在有以下问题，帮我解决下
1、现在卡片可以拖动到任意一列了，但是必须要拖动到目标列的卡片边缘才能触发，我希望能有直观的反馈，然后只需要拖动到目标列的空间就可以放开鼠标了
---
问题：
1、背景模糊设置时，边框泛白，能不能不要边框泛白
2、顶部搜索栏切换搜索引擎时，点击右侧的小三角图标没有，点击左边的放大镜才有效，而且弹出的菜单却是在右侧小三角下方，我希望直接删除小三角，然后弹出菜单移动到放大镜下方
3、左上角分类，目前是每个分类按钮里有个加号按钮用以新增分类，很奇怪，只需要在分类标签最右侧有一个+号按钮即可

5、卡片下方鼠标拖拽的区域悬浮效果太丑了，能不能改为一条横杠，毛玻璃效果，然后两端是圆角的。现在很突兀。
6、卡片里的子项右侧不是有个悬浮按钮吗，我希望也能在卡片内部拖动排序
7、现在卡片的左上角的根据内容适应高度的按钮图标不直观，能不能换一个直观的，而且根据开关状态显示不同的图标
8、现在卡片拖动位置时，没有一个直观的效果，比如同一列调整上下顺序时，做成像卡片里的子项一样，有一个横条直观的显示位置
9、现在卡片的紧凑模式是单独的菜单里控制，在右上角添加一个全局的按钮，同时调整所有的卡片的紧凑模式


---
- 在空白处右键弹出菜单，可以新建卡片，卡片的位置生成在右键的那一列位置
- 卡片选择文件夹时，默认所有文件夹都展开了，我希望默认全部折叠。另外当文件夹内没有文件夹的时候，左侧不要显示下拉箭头图标。
- 整理书签页面里，子项可以拖动上下位置排序，和卡片里的子项拖动位置一样
- 整理书签页面里，右侧面板里的文件夹下方的显示里面有几项全部都显示的0项，修复一下。
- 整理书签页面里，右键左侧导航菜单里的文件夹弹出菜单，删除和编辑操作，右侧内容里的文件夹和书签也一样支持右键编辑或删除
- 设置的自定义壁纸根据页面大小自动填充所有区域
- 整理书签页面我希望能左右分别各有一个导航栏和多页内容页支持，方便整理，类似左右分页
- 现在书签整理页面左侧导航栏的文件夹左侧有个文件夹图标然后会根据折叠状态切换图标，我希望这个文件夹图标固定。然后变化的应该是再左侧的箭头图标，展开后箭头朝下，折叠时箭头朝右侧。另外如果文件夹内没有其他可展开的文件夹了就不要显示箭头了
- 整理书签页面图标优化
- 主页面图标优化
- 主页面右侧空白处新建空白卡片，然后空白的卡片里有按钮可以选择文件夹
- 底部显示@firestaradmin
- 将右上方设置界面不要单独弹出一个页面，我希望能直接从右侧移出一个界面，方便用户直接预览修改的效果
- 主页图标设置
- 内置预设壁纸
- 导出配置，和恢复配置，一步到位包含书签
- 设置页面添加一个需要导入导出的文件夹设置，让用户选择，下次导入的时候就不需要用户选择要往哪个文件夹导入了，直接读取配置文件里的设置
- 优化设置页面里的预设壁纸，将其融合到上方的几个按钮里
- 支持明亮、黑暗模式切换，同时导入导出配置时也要保存和加载这个参数
- 设置页面里下拉菜单背景和文字色看不清，根据黑暗模式和明亮模式自动设置
- 现在我希望增加同步功能支持同步到Web Dev服务器上，需要设置地址和用户名以及密码，然后在设置面板中增加同步设置，用户可以选择定时备份或者检测设置更改或者书签被修改触发备份或者手动上传或下载。上传和下载的配置文件就和导入导出的文件一样
todo：
- 书签图标设置（来源、没有时直接显示开头字母）



---
- 我说了不需要兼容firefox了，你现在不用管firefox怎么写的，你直接就是按照原生的chrome写法，全部重写整个程序，哪里需要改就改哪里，不要纠结兼容firefox，你直接无视firefox就行，其他地方可能也有问题，你全部检查一遍
- 找到根因了：Chrome 返回的书签节点没有 type 字段（type= undefined）。所以 node.type === 'folder' 永远不成立 → 0 个节点被加进树。

这是 Firefox 和 Chrome 的 API 差异：Firefox 的 BookmarkTreeNode 有 type 字段，但 Chrome 的 chrome.bookmarks 节点没有 type 属性。Chrome 区分文件夹/书签的方式是：文件夹没有 url 字段，书签有 url 字段。
---

- 这是一个在firefox下开发的扩展程序，主要作用是平铺卡片式书签展示，和书签整理功能。现在我需要全面移植到chrome浏览器中，但是有很多函数用法不同，所以需要你全面修改代码以支持chrome。
目前部分已经移植为了chrome浏览器了，但是还有问题，比如Chrome 返回的书签节点没有 type 字段（type= undefined）。所以 node.type === 'folder' 永远不成立 → 0 个节点被加进树，导致显示不了文件夹等等。
现在我需要你完全放弃兼容firefox浏览器，不要以打补丁的方式去修改代码，而是以重构重写为手段，全方面移植为chrome扩展，完全以chrome扩展原生写法为准来修改代码。如果有必要也可以完全重写所有代码。另外如果你觉得完全文件结构等需要优化也可以自行做主决定（比如目前大部分代码都写在了newtab中，不方便修改维护扩展）。