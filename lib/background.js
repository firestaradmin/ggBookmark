/* GG Bookmark - MV3 background service worker entry */
/* Loaded by manifest "background.service_worker". Imports shared libs so the
 * same files can be reused in page contexts (which load them via <script>). */
importScripts('browser-polyfill.min.js', 'store.js', 'sync.js');
