# GG Bookmark

> **为 Google Chrome 打造的高性能书签起始页。**

GG Bookmark 将新标签页打造成一个美观、专注的书签指挥中心。它基于最新的 Web 平台标准构建，采用磨砂玻璃设计语言，在优雅的外观与 Chrome 原生 API 的速度之间取得平衡。

---

## ✨ 概览

| | |
|---|---|
| **平台** | Google Chrome / Edge（Chromium） |
| **架构** | Manifest V3，零运行时依赖 |
| **设计** | 磨砂现代化玻璃 UI，自适应深色主题 |
| **隐私** | 数据本地保存；可选自建 WebDAV 同步 |

GG Bookmark 致力于让操作毫不费力：即时搜索、可视化整理、安全同步——全程无需离开浏览器。



## 🚀 核心功能

### 新标签页起始页
- **统一搜索栏** — 在 Bing、百度、Google、DuckDuckGo、搜狗、GitHub 之间无缝切换。
- **分类网格卡片** — 书签以优雅的玻璃卡片呈现，每张卡片绑定一个书签文件夹。
- **灵活的卡片布局** — 拖动卡片标题排序、调整卡片高度、控制列数，适应任意屏幕。
- **智能紧凑模式** — 空间紧张时，卡片自动将标题与域名折叠为单行。
- **快速整理** — 在起始页直接一键整理书签。
- **favicon图标获取** - 支持直接快速使用api获取网页的图标

### 书签整理器
- **文件夹树导航** — 通过响应式侧边栏进入任意文件夹。
- **多选与拖拽** — 使用 Ctrl/⌘ 与 Shift 多选，移动或排序书签。
- **完整增删改查** — 创建、重命名、移动、删除文件夹与书签。
- **撤销支持** — 一键撤销误操作。

### 设置面板
- **背景系统** — 渐变、深色渐变、自定义图片（URL 或本地上传）、内置预设壁纸。
- **精细控制** — 微调模糊强度、遮罩透明度、玻璃透明度。
- **外观定制** — 深色 / 浅色主题、主题色、字体大小、卡片密度。
- **搜索引擎** — 选择默认引擎。

### WebDAV 同步
- **加密存储** — 密码在保存前加密处理。
- **灵活触发** — 支持定时、设置更改、书签更改、浏览器启动等多种触发方式。
- **完整日志** — 透明的上传 / 下载审计记录。



**更多好用的特性等你发掘**

## 界面预览

### 新标签页

![PixPin_2026-08-08_23-32-49](./assets/assets_README/PixPin_2026-08-08_23-32-49.webp)

### 设置页面

<img src="./assets/assets_README/PixPin_2026-08-08_23-33-08.png" alt="PixPin_2026-08-08_23-33-08" style="zoom:50%;" />

<img src="./assets/assets_README/PixPin_2026-08-08_23-33-16.png" alt="PixPin_2026-08-08_23-33-16" style="zoom:50%;" />

### 书签整理页面

![PixPin_2026-08-08_23-33-38](./assets/assets_README/PixPin_2026-08-08_23-33-38.png)



---

## 🛠 安装

> GG Bookmark 以未打包的扩展形式分发。这种方式让您的数据完全由自己掌控。

1. 打开 `chrome://extensions`（或 `edge://extensions`）。
2. 在右上角开启**开发者模式**。
3. 点击**加载已解压的扩展程序**，选择项目根目录。
4. 您的新标签页现由 GG Bookmark 驱动。随时点击工具栏图标即可返回起始页。

---

## 📁 项目结构

```
manifest.json            扩展清单（Manifest V3，Chrome 原生）
lib/browser.js           Chrome API Promise 封装 + 节点类型归一化
lib/store.js             默认设置、搜索引擎、书签工具
lib/sync.js              WebDAV 同步引擎（可由 alarm 在后台触发）
lib/background.js        MV3 service worker 入口
js/shared.js             共享图标与 Toast 通知
css/base.css             全局深色 / 磨砂玻璃主题
pages/newtab/            新标签页起始页 + 内嵌设置
pages/organizer/         书签整理器
icons/                   品牌与搜索引擎图标
pics/wallpapers/         内置预设壁纸
```

---

## 🔧 技术说明

### Chrome 书签节点模型
Chrome 的 `chrome.bookmarks` API 在返回的节点上**不含 `type` 字段**——这是与 Firefox 的关键差异。GG Bookmark 通过 `lib/browser.js` 对所有返回节点统一归一化，注入 `type`（`folder` / `bookmark`）。应用代码因此可以放心使用 `node.type === 'folder'` 判断，无需逐处处理。

### 隐私与数据
所有配置（分类、卡片、搜索设置）保存在浏览器本地存储。书签由浏览器自身的书签系统直接管理。除非您启用 WebDAV 同步到自己的服务器，否则没有任何数据会离开您的设备。

---

## ✅ 已完成功能

- 磨砂玻璃新标签页起始页，自适应浅色 / 深色主题
- 分类网格卡片，绑定书签文件夹
- 多搜索引擎支持（Bing、百度、Google、DuckDuckGo、搜狗、GitHub）
- 自定义背景：渐变、自定义图片（URL / 本地上传）、预设壁纸
- 模糊、遮罩透明度、玻璃透明度调节
- 卡片拖拽排序与单卡高度调整
- 紧凑模式，支持自动 / 固定高度
- 完整书签整理器：多选、拖拽、撤销
- WebDAV 同步：密码加密存储、多触发定时
- 完善的设置面板，支持配置导入 / 导出

---

## 📜 更新日志

### 第一期
- 自定义背景不再被模糊遮蔽，支持自定义模糊强度与透明度以完整呈现背景图
- 修复卡片右上角按钮图标不显示的问题
- 卡片子项支持整体高度调节：空间不足时域名自动移到标题同行并省略号显示，图标同步缩小
- 支持拖动卡片右下角调整卡片占用大小与宽度，同一列卡片宽度保持一致
- 新增按钮，根据子项数量自动调节卡片高度以显示全部子项

### 第二期
- 卡片设置菜单改为在右侧弹出，并加深背景提升可读性
- 卡片右下角拖拽改为仅上下调节，卡片宽度统一在全局设置中调整
- 支持一列放置多个卡片，卡片可堆叠于任意一列
- 修复设置背景图后仍不显示的问题

### 第三期
- 修复卡片右上角按钮弹出菜单被其他卡片遮挡的问题（置顶显示）
- 支持卡片自由拖拽，任意数量卡片均可排成同一列
- 自动适应后保持紧凑模式状态，不再自动关闭

### 第四期
- 卡片拖拽优化：拖到目标列空间即可释放，无需贴靠卡片边缘，并增加直观的落位反馈

### 第五期
- 修复背景模糊设置时边框泛白的问题
- 移除搜索栏右侧小三角，切换菜单统一在放大镜下方弹出
- 分类新增按钮改为仅在分类标签最右侧显示一个「+」
- 卡片底部拖拽区域改为毛玻璃圆角横杠，替代突兀的悬浮效果
- 支持在卡片内部拖动子项排序
- 卡片根据内容适应高度的按钮图标更直观，并根据开关状态切换
- 卡片拖拽落位增加直观横条反馈（同卡片子项拖拽）
- 右上角新增全局紧凑模式按钮，一键调整所有卡片

### 第六期
- 空白处右键菜单支持新建卡片，生成在右键所在列
- 卡片选择文件夹时默认全部折叠，无子文件夹时不显示箭头
- 整理书签页支持子项上下拖拽排序
- 修复右侧面板文件夹项数显示为 0 的问题
- 左侧导航与右侧内容均支持右键编辑 / 删除文件夹与书签
- 自定义壁纸按页面大小自动填充整个区域
- 整理书签页支持左右分栏导航与多内容页
- 左侧文件夹图标固定，折叠状态由箭头指示（展开朝下 / 折叠朝右），无子文件夹不显示箭头
- 整理书签页与主页图标优化
- 主页空白处支持新建空卡片，空卡片内可选择文件夹
- 底部显示 @firestaradmin
- 设置改为从右侧滑出面板，方便直接预览修改效果

### 第七期
- 内置预设壁纸
- 支持一键导出 / 恢复配置（一步到位包含书签）
- 新增导入导出目标文件夹设置，下次导入自动读取，无需重复选择
- 预设壁纸优化，融合进上方背景按钮
- 支持明亮 / 黑暗模式切换，导入导出时同步保存加载
- 下拉菜单背景与文字色根据主题自动适配
- 新增 WebDAV 同步：支持设置地址、用户名、密码，可定时备份、检测设置更改 / 书签修改触发、手动上传 / 下载
- 书签图标来源设置，无图标时显示标题首字母

### Chrome 重构
- 全面弃用 Firefox 兼容层，以 Chrome 原生 `chrome.*` API 重写
- 修复 Chrome 书签节点无 `type` 字段导致的文件夹无法显示问题（统一归一化注入 `type`）
- 重构文件结构，将起始页逻辑拆分维护

### 近期修复与新增
- 密码改为加密存储，避免明文保存
- 修复拖动卡片高度时首帧直接跳为两倍高度的问题
- 顶部工具栏下方新增置顶书签栏
- 合并书签 / 文件夹编辑浮动窗口与整理页右键编辑
- 新增新建文件夹窗口
- 优化整理页右侧面板，修复拖拽到面板文件夹无效、多选删除仅删一项、撤销等问题
- 右侧面板顶部新增路径链路，可点击返回任意上级目录
- 修复卡片内部子项拖拽排序问题
- 新增顶部工具栏、卡片与置顶行的磨砂模糊与透明度滑块
- 新增浏览器启动时自动触发同步机制
- 书签更改后延迟 30s 再上传，避免频繁上传
- 修复卡片选择文件夹时「包含子文件夹」选项无效的问题
- 修复右键置顶书签编辑后卡片与收藏夹不同步的问题
- 卡片宽度固定为设定值，页面宽度变化 / 刷新后布局保持不变
- 修复 tooltips 不显示的问题
- 置顶栏横向放不下时自动换行
- 新增卡片设置文件夹时若已存在对应卡片则提示
- 置顶栏新增清除按钮
- WebDAV 密码输入框样式与用户名统一，间隔改为滑动调节（步距 1 分钟，范围 1–120）

---

## 🗺 正在开发

### 🔴 高优先级（解决真实痛点）

1. **真正的双向同步（目前基本是单向上传）** — 当前定时 / 自动只上传，不会自动拉取远端。每次自动同步时先比对远端最后修改时间（WebDAV 的 `Last-Modified` / `Content-Length`），决定上传还是下载，避免本地被覆盖或丢失远端更新。可增加同步方向策略选项：仅上传 / 仅下载 / 双向。

2. **冲突检测与保护** — 双向同步时若本地与远端都改过会产生冲突。比对配置中的 `lastSyncAt` / 版本号，提示用户「本地较新 / 远端较新 / 冲突」，避免静默覆盖。

3. **书签与配置分离同步** — 当前把书签快照打包进配置，下载会重建书签（可能重复 / 冲突）。提供「是否同步书签」选项，默认只同步设置与卡片布局，书签单独可选手动处理，避免误删 / 重复书签。

### 🟡 中优先级（体验提升）

4. **首次配置引导 & 校验** — 当前密码 / 地址错误只在操作时报错。保存同步设置时自动测试连接并提示，避免配置错误后定时备份一直失败。

5. **更细粒度的同步内容选择** — 支持勾选同步哪些内容（设置 / 卡片布局 / 分类 / 书签），避免把本地壁纸等大文件或不想同步的内容上传。

6. **定时任务状态可视化** — 设置页显示「下次定时备份时间 / 上次成功时间」，让用户了解定时是否真正在运行。

7. **多份备份 / 版本保留** — WebDAV 上每次覆盖同名文件。提供可选的「版本化备份」（文件名带时间戳，如 `config-20260808.json`），保留最近 N 份，防止误操作。

---

## ⚠️ 无法实现

- 本地的文件同步机制

---

## 📄 许可证

© GG Bookmark. 保留所有权利。
