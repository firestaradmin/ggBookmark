
[只有在用户说需要提交时再阅读此文件]

# 🚀 发布流程说明（Release Process）

本仓库的版本发布遵循语义化版本（SemVer）迭代规则。以下为完整流程说明。

## 📌 版本号规则

版本号格式为 `major.minor.sub`（主版本.次版本.修订号），例如 `v1.0.2`。

迭代规则（三者互斥，按需选择其一）：

| 更新类型 | 示例改动 | 版本变化 |
| --- | --- | --- |
| **major**（主版本） | 破坏性重构、不兼容变更、整体架构调整 | `1.0.2` → `2.0.0` |
| **minor**（次版本） | 新增功能、非破坏性增强 | `1.0.2` → `1.1.0` |
| **sub**（修订号） | Bug 修复、小型优化 | `1.0.2` → `1.0.3` |

> 默认情况下，常规的 Bug 修复与细节优化走 **sub** 迭代；新增功能走 **minor**；仅重大重写才走 **major**。

## 🗂 版本号存放位置

版本号**统一以 `manifest.json` 的 `version` 字段为唯一权威来源**，只需修改这一处，其余位置均自动读取：

| 文件 | 版本号来源 | 说明 |
| --- | --- | --- |
| `manifest.json` | `version` | 唯一需要手动修改的权威版本号 |
| `lib/store.js` | `GG.VERSION` | 运行时从 `chrome.runtime.getManifest().version` 自动读取，**无需手动改** |
| 起始页页脚 | — | 运行时从 manifest 自动读取，**无需手动改** |

> 发布时只需修改 `manifest.json` 的 `version`，其它位置自动同步。

## 🔄 发布流程步骤

### 第 1 步：确认迭代类型并计算新版本号

- 查看 `manifest.json` 与 `lib/store.js` 中当前版本号。
- 根据本次更新内容确定迭代类型（major / minor / sub），计算出新版本号。
- 示例：当前 `1.0.2`，若为 Bug 修复则新版本为 `1.0.3`。

### 第 2 步：修改程序内显示的版本号

只需修改 `manifest.json` 的 `version` 字段为新版本；`lib/store.js` 与起始页页脚的版本号均会从 manifest 自动读取，无需手动同步。

### 第 3 步：将本次更新内容写入 CHANGELOG

- 在 `CHANGELOG.md` 顶部的导航目录中添加新版本锚点链接。
- 在新版本标题（`## v1.0.3`）中，以 `-` 列表简要描述本次更新内容。
- 条目需简洁、面向用户，可参照历史版本格式。

### 第 4 步：规范化提交（Commit）

提交信息需**规范化**，使用带前缀的格式，前缀准确概括本次更新类型，并简介说明更新内容。

常用前缀：

| 前缀 | 含义 | 示例 |
| --- | --- | --- |
| `feat:` | 新功能 | `feat: 新增卡片置顶功能` |
| `fix:` | Bug 修复 | `fix: 修复紧凑模式刷新所有卡片的问题` |
| `refactor:` | 重构（不改变功能） | `refactor: 重构渲染逻辑` |
| `perf:` | 性能优化 | `perf: 优化首屏加载速度` |
| `docs:` | 文档变更 | `docs: 更新 README` |
| `chore:` | 构建 / 工具 / 杂务 | `chore: 更新依赖` |
| `style:` | 代码格式（不影响逻辑） | `style: 调整缩进` |
| `test:` | 测试相关 | `test: 补充单元测试` |

示例提交信息：

```
fix: 点击紧凑模式按钮不再刷新其他所有卡片
```

### 第 5 步：自动提交

- 使用 `git add` 将相关改动文件（`manifest.json`、`lib/store.js`、`CHANGELOG.md`）加入暂存区。
- 使用规范化信息的 `git commit` 提交改动。
- 提交信息应概括本次更新内容（若本次包含多种改动，选择最主要的一类前缀）。

### 第 6 步：打 Tag

- 在提交完成后，打上版本标签，标签为**迭代后的版本号**，格式为 `v` + 版本号。
- 示例：新版本为 `1.0.2`，则标签为 `v1.0.2`。

```bash
git tag v1.0.3
```

### 第 7 步（用户选择）：推送远端

若需要同步远端，则推送提交与标签：

```bash
git push origin <branch>
git push origin v1.0.3
```
### 第 8 步（用户选择）：打包为zip

将本工程所有文件除了.gitignore里的以及.git文件夹的内容，按照最好的压缩率打包到packages文件夹下
命名规则 如ggBookMark_v1.0.2.zip


### 第 9 步（用户选择）：在git上发布release版本

将此次更新的距离上一个版本的更新内容全部写入release 说明里，然后附件是第8步打包的zip文件。然后发布版本


## ✅ 检查清单

发布前核对以下内容：

- [ ] 新版本号已更新到 `manifest.json`（`lib/store.js` 与页脚自动同步读取）
- [ ] `CHANGELOG.md` 已添加新版本条目及导航链接
- [ ] Commit 信息已规范化（带 `feat:` / `fix:` 等前缀并简介内容）
- [ ] 已提交本次改动
- [ ] Tag 已打上，名称为迭代后的版本号（如 `v1.0.3`）
